{
  "name": "employees-hub",
  "private": true,
  "workspaces": [
    "apps/web",
    "apps/api"
  ],
  "engines": {
    "node": "24.20.0"
  },
  "scripts": {
    "test:web": "npm --workspace employee-hub-web test -- --watch=false && vitest run scripts",
    "test:api": "npm --workspace employee-hub-api test -- --run",
    "test": "npm run test:web && npm run test:api"
  },
  "devDependencies": {
    "@types/jest": "30.0.0",
    "jest": "30.0.5",
    "ts-jest": "29.4.3",
    "typescript": "5.9.2",
    "vitest": "3.2.4"
  }
}
